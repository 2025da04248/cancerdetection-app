# Machine Learning Assignment 2 — Classification Models & Streamlit Deployment

**Course:** M.Tech (AIML/DSE) — Machine Learning, BITS Pilani WILP

## a. Problem Statement

Implement and compare multiple classification models on a single dataset,
expose them through an interactive Streamlit web application, and deploy
the app publicly on Streamlit Community Cloud.

## b. Dataset Description

- **Name:** Breast Cancer Wisconsin (Diagnostic) Data Set
- **Source:** `sklearn.datasets.load_breast_cancer` (originally UCI Machine
  Learning Repository)
- **Instances:** 569
- **Features:** 30 numeric features computed from digitized images of a
  fine needle aspirate (FNA) of a breast mass (e.g. mean radius, mean
  texture, mean smoothness, worst concavity, etc.)
- **Target:** Binary — `0` = malignant, `1` = benign
- **Class balance:** 212 malignant / 357 benign

> Replace this section if you choose a different dataset — keep the same
> structure (name, source, #instances, #features, target description).

## c. GitHub Repository Link

`<PASTE YOUR GITHUB REPO URL HERE>`

## d. Models Used

Train/test split: 80/20, stratified, `random_state=42`.
Logistic Regression and kNN use standardized features; Decision Tree,
Naive Bayes, and Random Forest use raw features.

| ML Model Name | Accuracy | AUC | Precision | Recall | F1 | MCC |
|---|---|---|---|---|---|---|
| Logistic Regression | 0.9825 | 0.9954 | 0.9861 | 0.9861 | 0.9861 | 0.9623 |
| Decision Tree | 0.9123 | 0.9157 | 0.9559 | 0.9028 | 0.9286 | 0.8174 |
| kNN | 0.9561 | 0.9788 | 0.9589 | 0.9722 | 0.9655 | 0.9054 |
| Naive Bayes | 0.9386 | 0.9878 | 0.9452 | 0.9583 | 0.9517 | 0.8676 |
| Random Forest (Ensemble) | 0.9561 | 0.9931 | 0.9589 | 0.9722 | 0.9655 | 0.9054 |

*(Generated by `train_models.py` — re-run it to reproduce/update these numbers.)*

### Observations

| ML Model Name | Observation about model performance |
|---|---|
| Logistic Regression | *<write 1–2 sentences in your own words: e.g. highest overall scores here — the classes are close to linearly separable in this feature space, and regularized LR generalizes well on 569 samples without overfitting.>* |
| Decision Tree | *<e.g. weakest performer — a single unpruned tree overfits the training split and loses recall on the minority (malignant) class.>* |
| kNN | *<e.g. solid performance once features are scaled; sensitive to the choice of k and to irrelevant/correlated features.>* |
| Naive Bayes | *<e.g. good AUC despite the independence assumption being violated (many features here are correlated), but slightly lower accuracy than LR.>* |
| Random Forest (Ensemble) | *<e.g. matches kNN on most metrics and is more robust than the single Decision Tree, as expected from averaging many trees.>* |
| **Overall Winner for your dataset?** | *<state which model you'd pick and why — e.g. Logistic Regression, for best MCC/AUC combined with being the simplest, most interpretable model here.>* |

> **Important:** Fill these observations in your own words after you look at
> *your* results — this is worth 3 marks and is exactly the kind of
> section graders check for generic/copy-pasted phrasing.

## Streamlit App

- **Live app:** `<PASTE YOUR STREAMLIT COMMUNITY CLOUD URL HERE>`
- Features: CSV upload of test data, model-selection dropdown, live
  evaluation metrics, confusion matrix, and classification report.

## Repository Structure

```
project-folder/
│-- app.py
│-- train_models.py
│-- requirements.txt
│-- README.md
│-- test_data.csv
│-- model/
    │-- logistic_regression.joblib
    │-- decision_tree.joblib
    │-- knn.joblib
    │-- naive_bayes.joblib
    │-- random_forest_ensemble.joblib
    │-- scaler.joblib
```

## How to Run Locally

```bash
pip install -r requirements.txt
python train_models.py     # trains models, writes test_data.csv + model/*.joblib
streamlit run app.py
```

## BITS Virtual Lab Execution

*<Insert your screenshot of running this on BITS Virtual Lab here, as required by the assignment.>*
